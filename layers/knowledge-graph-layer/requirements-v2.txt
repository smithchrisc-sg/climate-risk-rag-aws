# Knowledge Graph Layer Dependencies
# Version: 2.0.0 - NLP-Ontology Integration Extension
# Compatible with: Python 3.11, AWS Lambda

# === EXISTING DEPENDENCIES (UNCHANGED) ===
# Core RDF and SPARQL libraries
rdflib==7.0.0

# HTTP requests with AWS authentication
requests==2.31.0
requests-aws4auth==1.2.3
charset-normalizer==3.3.2

# AWS SDK
boto3==1.34.0
botocore==1.34.0

# === NEW NLP-ONTOLOGY DEPENDENCIES ===
# Fuzzy string matching for entity-concept alignment
fuzzywuzzy==0.18.0
python-Levenshtein==0.21.1

# Semantic similarity for contextual matching
sentence-transformers==2.2.2

# Fast pattern matching for ontology-driven annotation
ahocorasick-rs==0.20.0

# Text processing and normalization
nltk==3.8.1

# Scientific computing (required by sentence-transformers)
numpy==1.24.3
scipy==1.10.1

# Machine learning utilities (required by sentence-transformers)
scikit-learn==1.3.0

# Tokenization and text processing (required by sentence-transformers)
tokenizers==0.13.3
transformers==4.30.2

# PyTorch (CPU-only for Lambda) - required by sentence-transformers
torch==2.0.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu
torchvision==0.15.2+cpu --extra-index-url https://download.pytorch.org/whl/cpu

# === FUTURE SEARCH DEPENDENCIES (LIGHTWEIGHT) ===
# Result ranking and scoring utilities
pandas==2.0.3

# === STANDARD LIBRARY DEPENDENCIES (DOCUMENTED) ===
# The following are included in Python standard library:
# - json: JSON handling
# - logging: Logging utilities  
# - os: Operating system interface
# - time: Time-related functions
# - typing: Type hints
# - datetime: Date and time handling
# - hashlib: Hash algorithms
# - re: Regular expressions
# - urllib.parse: URL parsing
# - collections: Specialized container datatypes
# - itertools: Iterator utilities
# - functools: Higher-order functions
# - string: String operations
# - math: Mathematical functions
